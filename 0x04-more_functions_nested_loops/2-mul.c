#include "main.h"
#include <stdio.h>

/**
 * mul - hold the valude that need to be multiply.
 *
 * @a: The the value to multiply.
 * @b: the value to multiply with value a.
 *
 * Return: result success.
 */
int mul(int a, int b)
{
	return (a * b);
}
